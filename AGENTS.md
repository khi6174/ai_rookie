# SafeRoute AI 프로젝트 가드레일

## 문서 상태

- 상태: Approved
- 담당: 팀 안전빵
- 최종 갱신: 2026-07-14
- 적용 범위: 이 저장소의 모든 문서, 코드, 데이터, 테스트, 화면 및 데모

## 1. 미션

SafeRoute AI는 라스트마일 배송기사가 현재 남은 배송계획을 계속 수행할 때 동적 안전한계를 언제, 어디서 초과할지 예측하고, 원인을 설명하며, 실행 가능한 개입안을 비교한 뒤 기사 동의와 관리자 승인을 거쳐 배송계획을 갱신하는 안전운영 코파일럿이다.

제품의 핵심 성공 기준은 다음 폐루프가 실제로 이어지는 것이다.

> 미래 임계치 초과 예측 → 원인 설명 → 개입 비교 → 위험전가 검사 → 기사 동의 → 관리자 승인 → 경로·순서·ETA 갱신 → 고객 안내 → 전후 지표와 감사기록

기능의 수보다 이 폐루프의 재현성, 설명 가능성, 안전 형평성을 우선한다.

## 2. 최우선 제품 원칙

- SafeRoute는 사고확률을 예측하는 의료·보험·징계 시스템이 아니다.
- 결과는 `운영 위험지수`, `Safety Budget`, `Time-to-Breach`, `신뢰도`, `결측 데이터`로 표현한다.
- 안전은 ETA·거리·비용과 교환 가능한 가중치가 아니라 하드 제약이다.
- 안전하지 않은 후보를 먼저 제외하고, 안전한 후보 안에서만 ETA·비용·운영복잡도를 비교한다.
- 한 기사의 위험을 다른 기사에게 옮기지 않는다. 모든 물량이관은 수신 기사의 이관 후 Safety Budget 제약을 통과해야 한다.
- 기사와 관리자는 같은 결정 근거를 역할에 맞는 언어로 확인한다.
- 기사는 불이익을 암시하는 표현 없이 동의, 수정 요청, 거절, 이의제기를 할 수 있어야 한다.
- 관리자 화면은 기사 순위나 성과평가가 아니라 향후 지원이 필요한 상황과 실행 가능한 조치를 중심으로 구성한다.
- 원시 생체정보와 불필요한 정밀 이동궤적을 관리자에게 노출하지 않는다.
- 합성·Mock 데이터와 시뮬레이션 결과를 실제 운영 데이터나 사고감소 효과처럼 표현하지 않는다.

## 3. MVP 우선순위

### P0 — 반드시 하나의 폐루프로 동작

1. Dynamic Safety Envelope와 Safety Budget
2. 배송지별 예측과 Time-to-Breach
3. 위험 기여요인, 신뢰도, 결측 상태
4. 휴식·물량이관·순서변경·안전경로·Safe Delay의 반사실적 비교
5. Risk Transfer Guard와 실행 가능성 사유
6. 기사 동의 또는 수정·거절
7. 관리자 승인
8. 승인 후 경로·배송순서·ETA·고객안내 갱신
9. 조정 전후 지표와 감사기록

### P1 — P0 폐루프를 강화할 때만 포함

- Upstage 문서 파싱·정보 추출·근거 인용
- Near-miss 신고와 검증된 지역위험 반영
- Privacy/Audit 화면
- 평가 하네스와 30개 이상의 결정론적 합성 변형
- Live/Mock/Error/Fallback 상태와 데모 리셋

### P2 — 본선 MVP 이후

- 실제 사고확률 모델
- 개인별 생체 기준선 학습
- 대규모 실시간 관제 연동
- 다센터 운영 최적화
- 별도 고객 앱
- 제품 핵심과 무관한 장식성 대시보드 기능

P0가 검증되기 전에는 P1·P2 작업 때문에 P0 일정이나 복잡도를 늘리지 않는다.

## 4. AI 책임 경계

### 결정론적 코드가 소유하는 것

- Safety Budget과 운영 위험지수
- 위험 밴드와 임계치
- 위험 기여도
- 신뢰도와 결측값 판정
- Time-to-Breach와 예상 초과 배송지
- 개입 전후 효과
- 후보 실행 가능성
- Risk Transfer Guard
- 추천 후보의 순위와 최종 적용 상태

### Upstage 계층이 할 수 있는 것

- 안전 매뉴얼과 합성 보고서 파싱
- 허용된 필드의 구조화 추출
- 검증된 JSON과 인용문을 기반으로 한 역할별 설명
- 관리자 결정 메모, 기사 안내, 고객 안전지연 안내, 일일 요약 생성

### Upstage 계층이 해서는 안 되는 것

- 수치를 새로 계산, 추론, 반올림, 변경하거나 누락된 수치를 만들어 내는 행위
- 공급되지 않은 문서를 인용하는 행위
- 기사를 비난하거나 성과를 평가하는 행위
- 결정론 엔진의 추천이나 실행 가능성 판정을 덮어쓰는 행위
- 실패한 실연동을 정상적인 Live 결과처럼 표시하는 행위

모든 AI 출력은 스키마를 검증한다. 잘못된 응답, 타임아웃 또는 네트워크 장애 시에는 명시적인 Fallback 상태로 전환하고 `Demo fixture`임을 화면에 표시한다.

## 5. 사용자 경험과 디자인 경계

### 관리자 웹

- 방향: Calm Control Tower
- 첫 질문: “향후 60분 안에 누구에게 어떤 지원이 필요한가?”
- 오프화이트 바탕, 네이비 구조, 틸 행동, 앰버 대기, 빨강은 실제 임계치 초과에만 사용한다.
- 지도, Time-to-Breach, 개입 큐, 조정 전후 비교와 승인 근거를 우선한다.
- 기사 순위, 경쟁 배지, 저성과 표현, 원시 생체정보를 금지한다.

### 기사 PWA

- 방향: Field-first Human UI
- 한 화면에서 하나의 결정을 요구한다.
- 남은 안전여유보다 `몇 시·몇 번째 배송지까지 안전한가`를 함께 우선 표시한다.
- 최소 44px 터치영역, 키보드 접근성, 색상 외 상태표현, reduced-motion을 지원한다.
- 운전 중 긴 읽기나 텍스트 입력을 요구하지 않는다.
- 동의, 수정 요청, 거절을 비징벌적으로 제공한다.

### 공통 금지사항

- 상태를 색만으로 전달하지 않는다.
- 근거 없는 장식 차트, 과도한 네온·글래스모피즘·모션을 사용하지 않는다.
- 실제 길이의 한국어 대신 lorem ipsum을 사용하지 않는다.
- 관리자와 기사에게 서로 다른 수치나 상충하는 결정 근거를 보여주지 않는다.

## 6. 데이터와 개인정보 가드레일

- 모든 데이터에는 `live`, `mock`, `public-data-derived`, `user-entered` 등 출처 메타데이터를 둔다.
- Live/Mock/Error/Loading/Fallback 상태는 명시적인 구분 가능한 상태로 표현한다.
- 실제 개인식별 가능한 기사 데이터는 fixtures, 테스트, 스크린샷, 로그에 넣지 않는다.
- 입력 단위, 범위, 시간대, 결측 가능 여부와 보존 목적을 데이터 계약에 기록한다.
- 관리자에게는 의사결정에 필요한 파생 상태만 제공하고 원시 심박·수면·정밀 궤적을 제공하지 않는다.
- Near-miss 위치는 관리자 화면에서 운영상 필요한 수준으로 거칠게 처리한다.
- Near-miss 신고는 신고한 기사에게 즉시 불이익이나 위험점수 상승으로 돌아가면 안 된다.
- 검증된 신고만 향후의 지역화된 경로위험 특징에 반영한다.

## 7. 권장 기술과 구조

- 기본 스택: React, TypeScript, Vite, Tailwind CSS, Vercel Functions, Zod, Vitest, Playwright
- 계산 엔진, 개입 엔진, AI 설명 계층, UI 상태를 분리한다.
- 시스템 경계의 사용자 입력, 외부 API 응답, 문서 추출 결과를 검증한다.
- 핵심 계산은 순수하고 결정론적으로 유지하며 버전이 있는 설정파일에서 가중치와 근거를 관리한다.
- 일반화 가능성이 증명되지 않은 추상화나 미래용 인프라를 미리 만들지 않는다.
- 관련 없는 코드를 리팩터링하지 않는다.
- 가장 단순한 구조로 현재 수용기준을 만족시킨다.

구체적인 기술 선택은 `docs/architecture.md`와 `docs/decisions.md`가 승인된 이후 그 문서를 따른다.

## 8. 작업 전 필수 절차

모든 작업자는 수정 전에 다음 순서를 따른다.

1. 이 파일을 읽는다.
2. 작업과 관련된 승인 문서를 읽는다.
3. 저장소의 실제 파일과 현재 실행 상태를 확인한다.
4. 작업 범위, 수용기준, 비목표를 확인한다.
5. 기존 변경사항을 보존하고 관련 없는 파일을 수정하지 않는다.

행동의 기준이 되는 문서는 다음과 같다.

- `docs/product-spec.md`
- `docs/data-contracts.md`
- `docs/safety-model.md`
- `docs/intervention-policy.md`
- `docs/privacy-and-ai-policy.md`
- `docs/design-system.md`
- `docs/architecture.md`
- `docs/evals.md`
- `docs/demo-script.md`
- `docs/decisions.md`

문서가 아직 없거나 `Draft`인 경우, 불명확한 요구를 임의로 제품 동작으로 확정하지 않는다. 안전·개인정보·사용자 권리에 영향을 주는 결정은 문서에서 먼저 승인한다.

## 9. 구현 순서

다음 순서를 기본으로 한다.

1. 제품 명세와 비목표
2. 데이터 계약과 결정론적 fixtures
3. Safety Budget·Time-to-Breach 엔진
4. 개입 엔진과 Risk Transfer Guard
5. 관리자 폐루프
6. 기사 동의 폐루프
7. Upstage 문서·설명 계층
8. Near-miss 피드백
9. 평가 하네스
10. 3분 데모와 독립 검증

선행 단계의 수용기준이 충족되지 않은 상태에서 화면을 완성된 제품처럼 포장하지 않는다.

## 10. 검증과 완료 기준

- 핵심 계산에는 경계값, 정확한 예측값, 회복, 결측, 단조성 테스트를 둔다.
- 강수량·연속근무·남은 작업량·경로위험 증가가 Budget을 개선해서는 안 된다.
- 휴식 증가가 회복량을 악화해서는 안 된다.
- 결측 데이터 증가가 신뢰도를 높여서는 안 된다.
- 물량이관 후 수신 기사가 최소 안전범위를 벗어나는 후보는 실행 불가여야 한다.
- AI 문구가 바뀌어도 점수, 실행 가능성, 선택 개입은 바뀌어서는 안 된다.
- 관리자와 기사 E2E는 동일한 결정 ID와 근거를 사용한다.
- API 장애, 잘못된 AI 응답, 빈 데이터, 로딩, Fallback, 데모 리셋을 검증한다.
- 관리자 1440×900 및 1280×720, 기사 390×844 및 360×800에서 핵심 동작을 검증한다.
- 접근성 상태는 색상 외 텍스트나 아이콘을 포함하고, 키보드와 포커스를 검증한다.

작업 완료를 주장할 때는 해당 세션에서 확인한 테스트 결과, 빌드 결과, 스크린샷 또는 파일 diff를 제시한다. 실행하지 않은 검증과 실패한 검증을 숨기지 않는다.

## 11. 문서 운영 규칙

- 각 문서는 상태, 담당, 최종 갱신일, 결정사항, 미결사항, 수용기준, 비목표를 포함한다.
- `Draft`는 논의 중, `Approved`는 구현 기준, `Superseded`는 폐기된 기준을 의미한다.
- 지속되는 제품·행동·아키텍처 결정만 `docs/decisions.md`에 날짜와 함께 기록한다.
- 결정 기록에는 결정, 이유, 기각한 대안, 영향을 받는 파일을 포함한다.
- 재사용 가능한 교훈은 `docs/lessons/`에 주제별 한 파일로 기록한다.
- 구현과 문서가 충돌하면 임의로 한쪽을 정답으로 간주하지 말고, 사실을 확인한 뒤 승인된 문서 또는 결정 기록을 갱신한다.

## 12. 변경 통제

- 요청 범위를 넘어서는 기능 추가와 리팩터링을 금지한다.
- 새로운 의존성은 현재 수용기준을 충족하는 데 필요한 경우에만 추가한다.
- 안전 임계치, 가중치, 개인정보 범위, 동의 흐름, AI 권한을 바꾸는 작업은 반드시 문서와 결정 기록을 먼저 갱신한다.
- 데모 마감 72시간 전부터 신규 기능을 추가하지 않고 재현성, 오류 상태, 접근성, 발표 해상도, 리셋만 수정한다.
- 독립 검증자는 구현자의 완료 주장을 신뢰하지 않고 요구사항을 코드·테스트·화면까지 직접 추적한다.

## 13. 현재 미결사항

다음 내용은 후속 문서에서 확정한다.

- Safety Budget의 초기값, 임계치, 위험 밴드와 데모 수치
- 신뢰도 계산과 결측 입력의 영향
- 기사 거절·수정 요청 후 상태 전이
- 관리자 승인 생략이 가능한 조치의 범위
- Near-miss 위치 정밀도와 검증 절차
- 지도 공급자와 Live 데이터 연동 범위
- Upstage 모델 및 API의 구체적 계약
- P0/P1 최종 범위와 2026-08-14 이전 기능 동결일

미결사항을 구현 편의에 따라 조용히 확정하지 않는다.
